# Pinned to verified versions so a future clone can't break on an upstream release.
# (The Evidence/node side is locked by reports/package-lock.json.)
dbt-core==1.11.6
dbt-duckdb==1.10.1
duckdb==1.4.4

# Synthetic source-data generator only — NOT needed to build the committed seeds.
pandas==2.3.3
numpy==2.4.0
Faker==39.1.0
