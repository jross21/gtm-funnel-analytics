name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Allow the deploy job to publish to GitHub Pages.
permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

# Opt the pinned actions into the Node 24 runtime (clears the Node 20 deprecation).
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # --- dbt: seed + run + test -> arcline.duckdb (fails CI on any test failure) ---
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Install dbt + dev tooling
        run: pip install -r requirements-dev.txt
      - name: dbt deps
        run: dbt deps
      - name: Lint SQL (sqlfluff)
        run: sqlfluff lint models/ tests/
      - name: dbt build (strict)
        # --warn-error promotes any dbt warning (incl. deprecations) to a build failure.
        run: dbt build --profiles-dir profiles --target dev --warn-error

      # --- Evidence: build the static site against the DuckDB file dbt just produced ---
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: reports/package-lock.json
      - name: Configure Evidence base path for GitHub Pages
        # Project Pages serve under /<repo>/, so inject the base path for this build only.
        env:
          REPO_NAME: ${{ github.event.repository.name }}
        run: |
          printf '\ndeployment:\n  basePath: /%s\n' "$REPO_NAME" >> reports/evidence.config.yaml
      - name: Build Evidence site
        working-directory: reports
        run: |
          npm ci
          npm run sources
          npm run build

      - uses: actions/upload-pages-artifact@v3
        with:
          path: reports/build

  deploy:
    # Publish only from main; PRs run the build job for validation only.
    if: github.ref == 'refs/heads/main'
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
