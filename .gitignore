# dbt
target/
dbt_packages/
logs/
# The warehouse is a build artifact — regenerated by `dbt build`, never committed.
*.duckdb
*.duckdb.wal

# Python
.venv/
__pycache__/
*.pyc
.python-version.local

# Evidence build artifacts are handled by reports/.gitignore (nested).

# OS / editors
.DS_Store
.idea/
.vscode/

# Playwright MCP scratch output
.playwright-mcp/
